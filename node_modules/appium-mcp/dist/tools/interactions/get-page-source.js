import { z } from 'zod';
import { getDriver } from '../../session-store.js';
export default function getPageSource(server) {
    server.addTool({
        name: 'appium_get_page_source',
        description: 'Get the page source (XML) from the current screen',
        parameters: z.object({}),
        annotations: {
            readOnlyHint: true,
            openWorldHint: false,
        },
        execute: async (args, context) => {
            const driver = getDriver();
            if (!driver) {
                throw new Error('No driver found. Please create a session first.');
            }
            try {
                const pageSource = await driver.getPageSource();
                if (!pageSource) {
                    throw new Error('Page source is empty or null');
                }
                return {
                    content: [
                        {
                            type: 'text',
                            text: 'Page source retrieved successfully: \n' +
                                '```xml ' +
                                pageSource +
                                '```',
                        },
                    ],
                };
            }
            catch (err) {
                return {
                    content: [
                        {
                            type: 'text',
                            text: `Failed to get page source. Error: ${err.toString()}`,
                        },
                    ],
                    isError: true,
                };
            }
        },
    });
}
//# sourceMappingURL=get-page-source.js.map