import { z } from 'zod';
import { getDriver, getPlatformName } from '../../session-store.js';
export default function installApp(server) {
    const schema = z.object({
        path: z.string().describe('Path to the app file to install'),
    });
    server.addTool({
        name: 'appium_install_app',
        description: 'Install an app on the device from a file path.',
        parameters: schema,
        execute: async (args) => {
            const { path } = args;
            const driver = await getDriver();
            if (!driver) {
                throw new Error('No driver found');
            }
            try {
                const platform = getPlatformName(driver);
                const params = platform === 'Android' ? { appPath: path } : { app: path };
                await driver.execute('mobile: installApp', params);
                return {
                    content: [
                        {
                            type: 'text',
                            text: 'App installed successfully',
                        },
                    ],
                };
            }
            catch (err) {
                return {
                    content: [
                        {
                            type: 'text',
                            text: `Failed to install app. err: ${err.toString()}`,
                        },
                    ],
                };
            }
        },
    });
}
//# sourceMappingURL=install-app.js.map